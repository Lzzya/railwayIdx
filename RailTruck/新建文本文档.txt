output$carriage_zhi<-renderText({
    x1<-input$km_input
    tm<-c(2016)
    carriage<-c(0)
    distance<-c(x1)
    new_data<-data.frame(tm,carriage,distance,stringsAsFactors=FALSE)
    total_data<-svmReg(df,carriage~distance,new_data)  
    len<-dim(total_data)[1]
    paste("支持向量机预测货运量为：",str(total_data[len,1]))
          
  }
  )